# Weather application database connection
# Used from inside the Astro / Airflow Docker environment.

DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=Weather_Data_Pipeline
DB_USER=postgres
DB_PASSWORD=change_me
